import { style } from '@vanilla-extract/css';

export const page = style({
  margin: '1rem',
  maxWidth: '60rem',
});

export const button = style({
  display: 'flex',
  alignItems: 'center',
  gap: '0.3rem',
});
